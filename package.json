{
  "name": "adventure-house",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "simulate:resolution": "tsc -p tsconfig.scripts.json && node .tmp-scripts/scripts/simulate-resolution.js",
    "simulate:acceptance": "tsc -p tsconfig.scripts.json && node .tmp-scripts/scripts/simulate-acceptance.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "playwright": "^1.58.2",
    "typescript": "^5.6.3",
    "vite": "^6.2.0"
  }
}
